# Installs SG CRM as a Windows service via NSSM.
# Placeholder — implemented in phase 6 (productionize).
Write-Host "install-service.ps1 is a phase 6 placeholder. Run the server with 'npm start' for now."
